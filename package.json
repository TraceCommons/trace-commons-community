{
  "name": "trace-commons-community",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/fetch-snapshot.mjs && astro dev",
    "build": "node scripts/fetch-snapshot.mjs && astro build",
    "build:live": "node scripts/fetch-snapshot.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "astro check",
    "fetch-snapshot": "node scripts/fetch-snapshot.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
